# form-data
This project demonstrates basic React.js concepts including components, props, and state management. I created a user data form, collected input values, passed the data to a parent component using props,  and displayed the submitted details on a separate dashboard page.
